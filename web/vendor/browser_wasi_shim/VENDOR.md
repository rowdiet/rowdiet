# Vendored: @bjorn3/browser_wasi_shim 0.4.2

Source: https://www.npmjs.com/package/@bjorn3/browser_wasi_shim (dist/ of the 0.4.2 tarball,
verbatim; MIT OR Apache-2.0 — both license texts alongside). Pinned per the verified Phase-3
research: npm latest since 2025-06-22. Instantiate reactors with `wasi.initialize(instance)` —
the upstream README documents only `start()`, which throws on reactor modules.
